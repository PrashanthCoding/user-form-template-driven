import { BubbleCancas } from './bubble-cancas';

describe('BubbleCancas', () => {
  it('should create an instance', () => {
    const directive = new BubbleCancas();
    expect(directive).toBeTruthy();
  });
});
